TODOs

It would be so cool if in the experience and project sections, whenever a technology was listed, it would double as a link to the "list of technologies I'm familiar with". Could even have a tooltip to show familiarity. 

Note that this would be a bit like a sliding window problem. You have a list of keywords (really key-phrases, given a technology can be multiple words like 'Jenkins CI') of varying length. You then iterate through the sentences to identify when one shows up. 

Would also be cool if you could filter to only certain technologies or tags for various projects. 
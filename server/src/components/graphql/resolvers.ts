import { projectResolver } from "../projects/projectResolver";
import { experienceResolver } from "../experiences/experienceResolver";

export const resolvers = [projectResolver, experienceResolver];
